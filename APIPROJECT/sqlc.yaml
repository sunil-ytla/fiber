version: "2"
sql:
  - engine: "postgresql"
    queries: ".db/queries"
    schema: ".db/schema.sql"
    gen:
      go:
        package: "db"
        out: "db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_empty_slices: true
        emit_interface: false